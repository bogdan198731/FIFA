package com.example.worldcup;

import org.junit.jupiter.api.Test;

class WorldcupApplicationTests {

    @Test
    void contextLoads() {
        // Placeholder smoke test — the full Spring context is not started
        // here so the build does not require a running database.
    }
}
